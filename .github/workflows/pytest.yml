name: Pytest
on:
  push:
    branches:
      - main
      - dev
  pull_request:
    branches:
      - main
      - dev

jobs:
  container-test:
    runs-on: ubuntu-latest
    container: ndclab/pepper_container:szegedi-1.0.0
    steps:
      - name: Activate pipe conda env
        run: $CONDA/bin/conda activate pipe 
      - name: Run pytest
        run: pytest scripts/tests/