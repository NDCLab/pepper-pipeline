name: Pytest
on:
  push:
    branches:
      - main
      - dev
  pull_request:
    branches:
      - main
      - dev

jobs:
  container-test:
    runs-on: ubuntu-latest
    container: ndclab/pepper_container:szegedi-1.0.0
    steps:
      - name: conda init shell
        run: conda init
      - name: Activate pipe conda env
        run: conda activate pipe 
      - name: Run pytest
        run: pytest scripts/tests/